Local-first only (LocalStorage + JSON export/import)

No accounts, no cloud

Mobile-friendly layout

Keep UI gentle, non-judgmental language

Creativity (CEI) treated as a signal, not a productivity KPI
